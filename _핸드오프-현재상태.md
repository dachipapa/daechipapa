# 대치파파 — 현재 상태 핸드오프 (2026-06-01 세션 종료)

> **이 zip이 정본(최종 소스 전체)이다.** 내일 작업 시작할 때 이 zip 하나만 올리면 됨. 패치 쪼가리는 다 버려도 됨 — 여기 전부 반영돼 있음.

## 오늘 완료(코드 반영 + 검증됨)
1. **계정 연결(account linking)** — `auth_identities`·`device_links` 테이블 + 콜백 재작성. (네 네이버·카카오 계정 한 덩어리로 통합 완료, 데이터로 확인.)
2. **로그인 카카오 단일화** — 로그인 화면에 카카오만. (네이버·구글 백엔드 콜백은 살아있고 UI만 숨김 → 나중에 버튼만 되살리면 부활.) 계정화면도 단순화.
3. **세션 유지(BUG-2)** — 새로고침/뒤로가기 시 `PARENT_ID` 기준으로 앱 유지.
4. **새로고침 깜빡임 제거** — 로그인 상태면 즉시 대시보드 표시 후 보정.
5. **학년 중복("고2 두 번") 수정** — `kidInfo`가 입시연도를 4자리 연도일 때만 "입시 YYYY"로 표시.
6. **탈퇴/관리자삭제 cascade** — `auth_identities`·`device_links`도 삭제(PIPA).
7. **Studio 프롬프트 INSERT 버그 수정** — 컬럼5/값5 일치.
8. **로그아웃 정상화** — PARENT 메모리·이름캐시 비우고 nav 갱신(전엔 이름이 안 사라졌음).
9. **이름 깜빡임 제거** — 로그인 이름을 캐시해 새로고침 즉시 표시('로그인'→이름 깜빡 없앰).
11. **동의 모달 '뿌옇게 멈춤' 버그 수정** — `.consent`/`.sheet` 오버레이가 `position:absolute`라 긴 랜딩에서 카드가 뷰포트 밖(문서 중앙)에 떠 배경만 보임 → `position:fixed`로 뷰포트 정중앙 표시.
12. **약관 동의 기억** — 로그아웃→재로그인 때 매번 약관 체크 다시 받던 것 수정. `agreedTerms`를 기기에 저장해 재로그인 시 필수동의 자동 체크(매번 안 받음). ※카카오 동의창 매번 뜨는 건 카카오 콘솔 '동의항목 필수동의'로 별도 처리.
13. **스크롤 버벅임 개선** — 고정 nav/topbar의 `backdrop-filter:blur(18px)`(스크롤마다 블러 재계산=끊김) 제거, 거의 불투명 배경으로 대체. (모달 블러는 탭 시에만 떠서 유지.)
14. **saveProfile 방어코드** — 서버 응답에 kid 없을 때 크래시 방지.
15. **무로그인 체험 제거** — '로그인 없이 먼저 체험하기' 삭제(익명 기기계정·데이터꼬임 원인). 이제 로그인(카카오) 필수.
16. **제품명 단품→브리핑 전면 교체** — app/internal/terms 전부(세션3 결정 미반영분 적용). 가격 동일(브리핑 ₩29,000·마일스톤 ₩79,000). ※'멤버십' 추가는 스펙 미확인 → 보류.
17. **입시 학년도(공식) 교정** — cohort를 수능시행연도→공식 '학년도(입학연도)'로. 현재연도 자동계산(중3 Y+4·고1 Y+3·고2 Y+2·고3·N수 Y+1). 표시 'YYYY학년도'. 용어 '코호트'→'입시 학년도'. cohort-fix.sql도 strftime 기준으로 교정(고2→2028학년도).
18. **카톡/SNS 링크 미리보기(OG) 추가** — index.html·app.html에 og:title/description/image 등 추가, 공유썸네일 `public/og-image.png`(1200×630) 생성. (기존엔 OG 태그가 아예 없어 미리보기 안 떴음.) ※배포 후 카카오 OG 캐시 초기화 필요(developers.kakao.com 도구) 또는 링크에 ?v=2.
**전수 랜덤 베타테스트:** 화면17·버튼82 무작위 6패스×2상태 → 코드에러 0, 죽은버튼 0, 오버레이 위치 정상.
10. **계열(track) 저장 버그 수정** — 프로필 칩 셀렉터가 계열 자리에 학년값을 넣던 버그(`.chips:first/last-of-type` 오작동) → 인덱스 기반으로 교정. "고2 고2" 표시의 진짜 원인. kidInfo도 track==grade면 안 보이게 방어.

## 배포 상태 (중요)
- **배포 완료(라이브 반영):** 계정연결, cascade, 프롬프트수정, 카카오단일화 → 카카오 로그인 정상 확인됨.
- **⚠️ 미배포(마지막):** app.html(위 전부 + 계열저장버그 수정 포함) + `cohort-fix.sql` + `track-fix.sql`. → **이 한 번 배포 필요.**
  ```
  wrangler pages deploy public --project-name daechipapa --branch production
  wrangler d1 execute daechipapa-db --remote --file=cohort-fix.sql
  git add -A && git commit -m "..." && git push
  ```

## 원격 D1 적용된 스키마(이미 실행됨)
schema.sql(기본), schema-sched, schema-auth, schema-auth2, schema-invites, **schema-link(적용됨)**, **backfill-link(적용됨 — 윤나리→네이버계정 통합)**.
**미적용:** `cohort-fix.sql`, `track-fix.sql`(배포 시 실행).

## 다음 할 일 (우선순위)
1. **프로필 정돈 한 묶음:** 이름(별명) 라벨 정리 / **검정고시 옵션 추가**(현재 수동폼에 없음) / **학교 실시간 커스텀 드롭다운**(현 datalist는 모바일 약함) / "코호트"→"입시연도" 용어 / **FINDING-A: 수동 입력 학교가 리포트 완성도에 반영 안 됨**(saveProfile에서 학교매칭 or profileStatus가 p.school 인정).
2. **토스 결제 실연동:** 주문생성 API + 결제위젯 + product→권한 + 환불. (키 받으면.) 현재 리포트는 전부 402(어드민만 우회).
3. (선택) 하드닝: admin status 문자열보간 파라미터화, publish 허용목록서 universities/programs 제외, requireAdmin fail-closed.

## 고정 상수 (불변)
- Pages 프로젝트 `daechipapa`, **배포 브랜치 `production` 필수**. GitHub `github.com/dachipapa/daechipapa`(오타), 브랜치 main.
- D1: name `daechipapa-db`, binding `DB`, id `2f158940-598b-4fe8-af51-6a79b83dc328`. Account `eeadf8e86ef79f11aff4d72f222662e0`. ADMIN_SECRET=admin123.
- 도메인: daechipapa.com(index) / app.daechipapa.com(app) / internal.daechipapa.com(internal, Cloudflare Access).
- 모델: 대화 sonnet-4-6 / 리포트 opus-4-8 / 추출·분류 haiku. 브랜드 #183367 + #e0922f, Pretendard.
- 배포 방식: 사용자가 로컬 `~/Downloads/daechipapa-backend`에서 `wrangler pages deploy public --project-name daechipapa --branch production` + git push. 스키마는 `wrangler d1 execute daechipapa-db --remote --file=...`.

## 참고
- 상세 구현/감사 명세: 같은 폴더 `daechipapa-구현명세-감사-v2.md` 참고(전수 점검판). 단 v2는 카카오단일화 결정 전 작성이라, 로그인 부분은 본 핸드오프가 최신.
- 로그인은 카카오 하나로 확정. CI/실명인증/멀티프로바이더 연결은 지금 도입 안 함(필요해지면 카카오싱크로 CI).
19. **카카오톡 간편로그인(JS SDK, app-to-app)** — 카톡 깔린 폰에서 탭 한 번에 로그인(매번 동의/로그인 화면 안 거침). 구현: app.html에 Kakao SDK(2.7.5) 로드 + `KAKAO_JS_KEY='0991f5ad01c457dd83cbe648bd5c2cb1'`(JS키, 도메인제한 클라이언트용) + socialLogin이 `/api/auth/kakao/start?mode=json`으로 서명 state·쿠키 받고 `Kakao.Auth.authorize` 호출. start.js에 mode=json 분기 추가(dp_oas 쿠키 심고 {state,redirectUri,scope} 반환). 콜백/계정연결 백엔드 무변경. SDK 실패 시 기존 REST 리다이렉트로 자동 fallback. **필요(콘솔): JS키의 'JavaScript SDK 도메인'에 app/대치파파 도메인, redirect URI=https://app.daechipapa.com/api/auth/kakao/callback 등록.** 첫 1회 동의는 불가피, 이후 즉시.
20. **간편로그인 state 실패 수정** — 콜백이 dp_oas 쿠키와 state를 '둘 다 일치' 강제 → SDK 경로에서 쿠키가 리다이렉트 사이 유실되면 매번 err=state. → **쿠키 있으면 일치 검사, 없으면 HMAC 서명(+만료+provider)으로만 검증**으로 완화(서명 state 자체가 CSRF 방어라 안전). callback.js line19.
※동의 모달 데스크톱 깨짐: 현재 코드는 모바일/데스크톱/스크롤 후 모두 정중앙 검증됨 → 라이브 스테일/캐시 문제. 배포 후 강력 새로고침(Ctrl+Shift+R) 필요.
※index.html 랜딩에 아직 '앱으로 시작'(186)+CTA들 → 단일 'カ카오로 시작' 정리 대기.
21. **입시 학년도 표시 = 학년에서 즉석 계산** — 기존엔 저장된 cohort 값을 그대로 표시 → 옛 아이(2027 저장)는 코드 고쳐도 계속 2027. 이제 kidInfo가 gradeYear(grade)로 매번 계산(고2→올해+2=2028). DB 값 무관하게 정확. cohort-fix.sql은 이제 선택(AI 분석용 저장값 교정).
22. **로그인 로딩 표시** — 카카오 로그인 탭 시/콜백 복귀 처리 중 풀스크린 스피너 오버레이(.loadov, '카카오 로그인 중…'/'로그인 중…'). '빙빙 돌다 멈춤'에 피드백 제공.
23. **랜딩 상단 버튼 '앱으로 시작'→'바로가기 설치'**(install-guide.html). (남은 랜딩 CTA 단일화는 추후.)
24. **모달 위치 안전장치(못박기)** — openConsent/askGenerate(genConsent)/openSheet가 모달을 열기 전 document.body로 이동(_toBody). 조상 transform/긴 페이지와 무관하게 항상 뷰포트 정중앙. (현재 코드는 이미 position:fixed로 정상이었으나, '한참 아래로' 재발 방지용 belt-and-suspenders.) ※재현 결과 현재 코드는 모바일/데스크톱/스크롤 끝 모두 centered — 사용자가 보는 깨짐은 라이브 스테일(옛 position:absolute) 의심.
※의심: 배포가 production 브랜치에 안 올라갈 가능성(--branch production 누락 시 preview로 감). Cloudflare Pages 대시보드 Production 배포 시각/커밋 확인 필요. 라이브 검증: app.daechipapa.com/app.html에서 '브리핑'·'2028학년도' 보이면 최신, '단품'·'2027'이면 스테일.
