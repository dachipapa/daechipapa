# 소셜 로그인 설정 체크리스트 (오후 작업용)

코드는 다 준비됨. 아래 **콘솔 등록 + 시크릿 등록**만 하면 켜집니다.
(미설정 provider는 `/api/auth/config`가 자동으로 숨겨서 버튼이 안 보임 — 카카오만 먼저 켜도 됨.)

## 0. 패치 적용 (코드)
```bash
cd ~/Downloads/daechipapa-backend
unzip -o ~/Downloads/daechipapa-auth-patch.zip -d .
# parents 테이블에 소셜 컬럼 추가 (최초 1회)
wrangler d1 execute DB --file=schema-auth.sql --remote
```

## 1. 공통 시크릿
```bash
# 랜덤 서명키 (state/CSRF 서명용) — 한 번만
wrangler pages secret put AUTH_SECRET   # 예: openssl rand -hex 32 결과 붙여넣기
```

## 2. 카카오 (1순위, 카카오싱크 권장)
- developers.kakao.com → 내 애플리케이션 → 앱 생성
- **플랫폼 > Web** 사이트 도메인: `https://app.daechipapa.com`
- **카카오 로그인** 활성화 ON
- **Redirect URI**: `https://app.daechipapa.com/api/auth/kakao/callback`
- **동의 항목**: 닉네임(profile_nickname) 필수, 이메일·연령대·출생연도는 선택(연령대/이메일은 비즈앱 전환·검수 필요할 수 있음)
- 키 등록:
```bash
wrangler pages secret put KAKAO_CLIENT_ID       # REST API 키
wrangler pages secret put KAKAO_CLIENT_SECRET    # (보안 > Client Secret, 사용함으로)
```

## 3. 네이버 (2순위)
- developers.naver.com → 애플리케이션 등록 → 사용 API: **네이버 로그인**
- 제공 정보: 이메일, 이름/별명, 연령대, 출생연도 체크
- 서비스 URL: `https://app.daechipapa.com`
- **Callback URL**: `https://app.daechipapa.com/api/auth/naver/callback`
```bash
wrangler pages secret put NAVER_CLIENT_ID
wrangler pages secret put NAVER_CLIENT_SECRET
```

## 4. 구글
- console.cloud.google.com → API/서비스 > OAuth 동의 화면 구성 → 사용자 인증 정보 > **OAuth 클라이언트 ID(웹)**
- 승인된 리디렉션 URI: `https://app.daechipapa.com/api/auth/google/callback`
```bash
wrangler pages secret put GOOGLE_CLIENT_ID
wrangler pages secret put GOOGLE_CLIENT_SECRET
```
- ⚠️ 구글 기본 프로필엔 생년이 없음 → 연령은 가입 시 '만 14세 이상' 자가 체크로 처리(이미 반영됨).

## 5. 배포 & 확인
```bash
wrangler pages deploy public --project-name daechipapa --branch production
git add -A && git commit -m "social login (kakao/naver/google) + 동의·연령게이트" && git push
```
- app.daechipapa.com → 시작하기 → 필수 동의 체크 시 버튼 활성 → 각 provider 로그인 → 대시보드 복귀 확인.
- 실패 시 주소창 `#err=...`(state/token/user)로 원인 표시.

## 참고
- 콜백은 `app.daechipapa.com` 기준. 도메인 바꾸면 각 콘솔 Redirect URI도 같이 수정.
- 체험(로그인 없이) 중 만든 아이는 소셜 로그인 시 그 계정으로 자동 이전됨.
- 정식 이용약관/개인정보 문구는 `legal` 화면에 요지만 넣어둠 → 변호사 검토 후 교체.
